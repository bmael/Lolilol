<UserControl x:Class="Control.Champion.View.AllChampions"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:componentModel="clr-namespace:System.ComponentModel;assembly=WindowsBase"
             xmlns:data="clr-namespace:Tools.Windows.Data;assembly=Tools"
             xmlns:viewModel="clr-namespace:Control.Champion.ViewModel"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300" d:DataContext="{d:DesignInstance Type=viewModel:AllChampionsModel}">

    <UserControl.DataContext>
        <viewModel:AllChampionsModel />
    </UserControl.DataContext>
    
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../TemplateDictionary.xaml"/>
                <ResourceDictionary>
                    <data:FirstLetterConverter x:Key="FirstLetterConverter" />
                    <CollectionViewSource x:Key="ContactCvs" Source="{Binding Path=Champions}">
                        <CollectionViewSource.SortDescriptions>
                            <componentModel:SortDescription PropertyName="Name" />
                        </CollectionViewSource.SortDescriptions>
                        <CollectionViewSource.GroupDescriptions>
                            <PropertyGroupDescription PropertyName="LastName" Converter="{StaticResource ResourceKey=FirstLetterConverter}" />
                        </CollectionViewSource.GroupDescriptions>
                    </CollectionViewSource>
                </ResourceDictionary>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>

    </UserControl.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition />
            <RowDefinition Height="25"/>
        </Grid.RowDefinitions>

        <ListBox Grid.Row="1" Grid.Column="0"
                 ItemsSource="{Binding Source={StaticResource ResourceKey=ContactCvs}}"
                 SelectedItem="{Binding Path=SelectedChampion}">
            <ListBox.GroupStyle>
                <GroupStyle>
                    <GroupStyle.ContainerStyle>
                        <Style TargetType="{x:Type GroupItem}">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="{x:Type GroupItem}">
                                        <Expander IsExpanded="True" Header="{Binding Path=Name}" >
                                            <ItemsPresenter />
                                        </Expander>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </GroupStyle.ContainerStyle>
                </GroupStyle>
            </ListBox.GroupStyle>
            <ListBox.Style>
                <Style TargetType="ListBox">
                    <Setter Property="Background" Value="LightGray" />
                    <Setter Property="ItemTemplate" Value="{StaticResource ResourceKey=ClassicTemplate}" />
                </Style>
            </ListBox.Style>
        </ListBox>

    </Grid>
</UserControl>
